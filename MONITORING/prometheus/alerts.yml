groups:
  - name: ml_service
    rules:
      - alert: HighAnomalyRate
        expr: sum(rate(anomaly_predictions_total{result="anomaly"}[1m])) > 0.1
        for: 30s
        labels:
          severity: warning
        annotations:
          summary: "Высокий поток аномалий"
